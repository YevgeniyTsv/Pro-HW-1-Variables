<html>
<head>
    <title>JAVASCRIPT</title>
</head>
<body>
    <script>
        // - это один из вариантов как комментировать что-то 
        // то есть строки где присутствуют два слэша интерпретатор пропускает

        const first = 10 && 9 && false && 5 && 6;
        // first=false
        //  Оператор && переберает значения до первого false
        //

        const second = 9 || false || 5 || 6 || 0;
        //  second=9
        //  Оператор  || переберает значения до первого true
        //

        const third = false && 9 || false && 5 && 6;
        //  third=false
        //  Оператор && переберает значения до первого false
        //  Оператор && имеет приоритет перед || запись будет иметь вид false || false, тоесть, || будет перебирать до последнего false

        const fourth = 10 && 9 || 3 || 5 && 6;
        // fourth=9
        //  сперва вычислится 9 далее 6 следовательно получаем запись вида : 9 || 3 || 6 далее || спотыкается о первый true, в нашем случае 9
        //

        const fifth = 1 || 5 && 6 && 4 && 1 && 343;
        // fifth=1
        //  сперва выполняется более приоритетный && и перебирает все значения до тех пор как не обнаружит false, 
        //  так как все что он перебирает это цифры а цифры это true (кроме 0) то мы имеем запись 1 || 343, спотыкаемся о первое значение true = 1
        //

        const sixth = 17 > 43 && true && '' || 1;
        // sixth=1
        //  тут выходит false && true && false || true
        //  в итоге false || true, || переберает значения до правды,следовательно 1

        const seventh = 1 && true && 34 > 17 || 1;
        // seventh=true
        //  тут выходит true (число больше  0) && true && true (действительно, 34 больше 17 это тру) || true (все числа кроме 0 тру)
        // в итоге получим запись true || 1 , спотыкаемся о первый тру

        const eighth = '' || 0 || 3 && 4 && 9 && 89;
        // eighth=89
        //  тут выходит false || false || true && true && true && true 
        //  Оператор && переберает значения до последнего true в списке и выполняется первый по приоритетности, следовательно у нас останется число 89
        //  и запись будет иметь вид     '' || 0 || 89      а это     false || false || true , в этом случае || перебирает все пока не найдет true, в нашем случае это 89

        console.log(first, second, third, fourth, fifth, sixth, seventh, eighth);

        //  Итак, оператор || вычисляет операнды слева направо до первого «истинного» и возвращает его, а если все ложные – то последнее значение.
        //  Итак, оператор && вычисляет операнды слева направо до первого «ложного» и возвращает его, а если все истинные – то последнее значение.
        //  && приоритетнее над ||
        //  || запинается на «правде»,
        //  && запинается на «лжи».
    </script>

            
</body>
</html>
